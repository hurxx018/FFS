# Fluorescence Fluctuation Spectroscopy (FFS) analyses in Python

The goal is to create a package in Python for analyzing photon count data.



# Dependencies
numpy, scipy

lmfit (conda install -c newville lmfit)
